
const config = {
  // updated: removed Lovable appId
  appName: 'StudentMate',
  webDir: 'dist',
  server: {
    // updated: removed Lovable url
    cleartext: true
  },
  plugins: {
    StatusBar: {
      style: 'DARK',
      backgroundColor: '#001F3F'
    }
  }
};

export default config;